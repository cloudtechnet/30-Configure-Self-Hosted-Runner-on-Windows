name: Windows Self Hosted Workflow

on: push

jobs:
  build:
    runs-on:
      - self-hosted
      - windowss

    steps:
      - name: Display Message
        run: echo "Running on Windows Self-Hosted Runner"
